{
  "name": "eduplas-onayservisi",
  "version": "1.0.0",
  "engines": { "node": "20" },
  "scripts": {
    "lint": "echo \"(opsiyonel)\"",
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
